export interface OwnRewardHistory {
    RewardID: number;        // Reward ID
    reward_name: string;      // Reward Name
    discount: number;         // Discount
    reward_points: number;    // Reward Points
    reward_type: string;      // Reward Type
    history_state: string;    // History State
    history_expire: string;   // History Expiration Date
    user_name: string;        // User Name
    image_path: string;       // Image Path
    barcode: string;          // Barcode
  }
  